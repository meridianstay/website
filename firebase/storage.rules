rules_version = '2';
// Uploads go through the Meridian Stay API. Photos are served through their public
// download links, so direct browser access stays closed.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
