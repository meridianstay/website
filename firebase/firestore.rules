rules_version = '2';
// All reads and writes go through the Meridian Stay API (Firebase Admin SDK), which enforces
// roles and business rules. Browsers get no direct database access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
